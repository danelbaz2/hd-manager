// Runtime configuration - can be overwritten at container startup
// Default values are used if not overridden
//
// In Docker: This file is regenerated by frontend-entrypoint.sh
// In Development: These values are used as defaults, but Vite env vars take precedence
window.__RUNTIME_CONFIG__ = {
  // Application settings
  SYSTEM_NAME: "Flow Task",
  API_URL: "/api",
  API_MAX_RETRIES: 2,
  API_RETRY_DELAY: 500,
  SOCKET_TIMEOUT: 10000,
  IDLE_TIMEOUT: 300000,
  // External system URLs
  SERVICENOW_URL: "https://servicenow.com",
  MARS_URL: "https://mars-system.com",
};
