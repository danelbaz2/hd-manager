// Runtime configuration - for local development
// In Docker, this file is replaced by frontend-entrypoint.sh at container startup
// The values here are just fallbacks for local dev when config.js doesn't exist

// This file is intentionally empty for dev mode -
// runtimeConfig.ts will fall back to Vite env variables or defaults
window.__RUNTIME_CONFIG__ = undefined;
